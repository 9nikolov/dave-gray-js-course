// Arrays
const  myArray = [];

// add elements to an array,
// by referencing the position they are in, in the array
myArray[0] = "Dave";
myArray[1] = true;
myArray[2] = 2093;

// refer to an array
console.log(myArray);