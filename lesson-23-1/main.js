// Web Storage API
// 1. Not part of the DOM - refers to the Window API
// Available to JS via the global variable: window
// 2. `We do not have to type window. It is implied/implicit:
// example (these 2 are the same thing):
window.alert("ok");
alert("ok");
// another example (these 2 are the same thing):
window.alert(window.location);
alert(location);
