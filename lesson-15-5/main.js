const myString = "The quick brown fox jumps over the lazy dog";

// This line splits the myString variable into an array of strings, 
// with each string being separated by a space (' ') character.
const myArray = myString.split(' ');

console.log(myArray);
